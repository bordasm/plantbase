# Adatvédelem / anonimizálás (F al-projekt) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Három független szállítmány: (1) adatfolyam-dokumentáció (mi utazik az Anthropic/OpenAI API-hoz, mi marad helyben); (2) egy staff-indított, irreverzibilis fiók-anonimizáló mechanizmus (`POST /api/staff/accounts/:id/anonymize`) + minimális staff-lista; (3) egy önálló script, ami a régi szimulált e-mail-fájlokat törli az `/emails` könyvtárból.

**Architecture:** Nincs új tábla az anonimizáláshoz — a meglévő `accounts` tábla kap egy `anonymizedAt` nullable mezőt. Az anonimizálás egyetlen tranzakcióban írja át a fiók PII-mezőit placeholderekre, teszi visszafejthetetlenné a jelszót, és törli a session-öket — az `orders`/`escalations`/`order_audit_log` sorok érintetlenek maradnak. A login-végpont explicit ellenőrzi az `anonymizedAt`-ot védelmi rétegként. Az e-mail-takarítás egy önálló, `tsx`-szel futtatható script (a seed-szkriptek mintájára), NEM egy szerver-integrált időzítő.

**Tech Stack:** Prisma, Express 5, bcryptjs (`hashPassword`, már létező), Node `node:crypto`/`node:fs`, React 19, Vitest, Prettier (`semi:false`).

**Spec:** `docs/superpowers/specs/2026-08-20-privacy-design.md`

## Global Constraints

- Az anonimizálás IRREVERZIBILIS és IDEMPOTENS — már anonimizált fiók újra-anonimizálása nem fut le, nincs mellékhatása.
- A jelszó visszafejthetetlenné tétele valódi bcrypt-hash-elésen alapul egy véletlen, sosem tárolt bemenetről (`hashPassword(randomUUID())`) — nem egy törölhető/megkerülhető flag.
- Az anonimizált fiók bejelentkezési kísérlete ugyanazt az általános "Hibás e-mail vagy jelszó." 401-üzenetet kapja, mint egy rossz jelszó — SOSEM különböző üzenetet (állapot-szivárgás elkerülése).
- Az `orders`/`escalations`/`order_audit_log` sorok anonimizáláskor ÉRINTETLENEK maradnak — csak maga a fiók (`accounts` sor) és a `sessions` törlődnek.
- Mindkét új staff-végpont (`GET /api/staff/accounts`, `POST /api/staff/accounts/:id/anonymize`) csak `staff`/`admin` szerepkörnek érhető el.
- Az e-mail-takarító script NEM fut le automatikusan modul-importáláskor (tesztelhetőség) — csak közvetlen `tsx`-futtatáskor.
- Prettier: `semi: false` mindenhol.

---

## Task 1: Prisma séma — `anonymizedAt` mező + migráció

**Files:**
- Modify: `packages/db/prisma/schema.prisma`
- Create: `packages/db/prisma/migrations/<timestamp>_add_account_anonymized_at/migration.sql`

**Interfaces:**
- Produces: az `Account` modell egy új, nullable `anonymizedAt: DateTime?` mezővel, amit Task 2 (`accounts-store.ts`) fog használni.

- [ ] **Step 1: `schema.prisma` bővítése**

Az `Account` modell (jelenleg `escalations Escalation[]` sorral záródik, `@@map("accounts")` előtt) kapjon egy új mezőt:
```prisma
model Account {
  id           Int      @id @default(autoincrement())
  fullName     String   @map("full_name")
  salutation   String
  email        String   @unique
  passwordHash String   @map("password_hash")
  role         String   @default("customer") // customer | staff | admin
  createdAt    DateTime @default(now()) @map("created_at")
  anonymizedAt DateTime? @map("anonymized_at")
  sessions     Session[]
  orders               Order[]
  orderAuditLogEntries OrderAuditLog[]
  escalations          Escalation[]

  @@map("accounts")
}
```
(Csak az `anonymizedAt` sor az új rész — a többi a jelenlegi tartalom, ellenőrizd a valódi fájlt, mielőtt módosítasz, és ehhez igazítsd, ha eltér.)

- [ ] **Step 2: Migráció létrehozása**

A repó dokumentált tapasztalata alapján (lásd `schema.prisma` runbook-kommentje) próbáld meg először:
```bash
pnpm --filter @plantbase/db exec prisma migrate dev --name add_account_anonymized_at --create-only
```
Ha ez a HNSW-index-checksum-drift hiba miatt elutasítja a futtatást, NE próbálj `prisma migrate reset`-et futtatni — ehelyett kézzel hozd létre a migráció-könyvtárat/fájlt pontosan abban a rétegben, amit a `--create-only` létrehozott volna (időbélyeges könyvtárnév, LF sortörés), a Step 1 sémaváltozásából Prisma által generált SQL-lel egyenértékű tartalommal (`ALTER TABLE "accounts" ADD COLUMN "anonymized_at" TIMESTAMP(3);`), majd alkalmazd `prisma migrate deploy`-jal.

- [ ] **Step 3: Élő ellenőrzés — `accounts` továbbra sem érhető el `plantbase_ro`-nak**

Az `accounts` tábla teljes hozzáférését a B al-projekt záró biztonsági javítási köre már visszavonta `plantbase_ro`-tól (ld. `20260818203503_restrict_readonly_role_from_accounts_sessions` migráció) — egy új OSZLOP hozzáadása egy MÁR letiltott táblához nem igényel új jogosultság-kezelést, de egy gyors ellenőrzés (nem teljes ceremónia, csak megerősítés) helyénvaló:
```bash
docker exec plantbase-postgres-1 psql -U plantbase_ro -d plantbase -c "SELECT count(*) FROM accounts;"
```
Elvárt: `permission denied for table accounts` (változatlanul, mint korábban).

- [ ] **Step 4: Prisma Client regenerálása + build**

```bash
pnpm --filter @plantbase/db exec prisma generate
pnpm exec nx run-many -t lint,build -p db
```
Expected: hiba nélkül.

- [ ] **Step 5: Commit**

```bash
git add packages/db/prisma/schema.prisma packages/db/prisma/migrations/
git commit -m "feat: add anonymizedAt field to accounts table"
```

---

## Task 2: `apps/server` — `accounts-store.ts`

**Files:**
- Create: `apps/server/src/lib/accounts-store.ts`
- Test: `apps/server/src/lib/accounts-store.spec.ts`

**Interfaces:**
- Produces:
  ```ts
  interface AccountSummary {
    id: number
    fullName: string
    email: string
    role: string
    anonymizedAt: string | null
  }
  type AnonymizeResult = 'ok' | 'not_found' | 'already_anonymized'
  function anonymizeAccount(accountId: number): Promise<AnonymizeResult>
  function listAccountsForStaff(): Promise<AccountSummary[]>
  ```
  Ezt fogja Task 3 (`staff-accounts.ts` route) importálni.
- Consumes: `hashPassword` (`../lib/password.js`, már létező); `prisma` (`@plantbase/db`).

**FONTOS (koncurrencia-megjegyzés, ne bonyolítsd túl):** ellentétben `orders-store.ts` `runSerializable`-jével (ami a rendelés-státusz race-eket kezeli), itt NEM kell `Serializable` izoláció vagy retry-logika. Ha két konkurrens anonimizálás-kísérlet ugyanazt a fiókot célozza READ COMMITTED alatt, mindkettő ugyanazokat a determinisztikus placeholder-értékeket (fullName/salutation/email az `accountId`-ból számolva) írná be — a végállapot idempotens, függetlenül attól, hogy 1 vagy 2 konkurrens tranzakció fut le ténylegesen. Ez különbözik a rendelés-státusz-race-től, ahol egy konkurrens írás egy ÜZLETI ÁLLAPOTOT (pl. "teljesítve") írhatna vissza helytelenül — itt nincs ilyen kockázat. Egyszerű `prisma.$transaction(async (tx) => {...})`, korai `return`-ökkel a "nem található"/"már anonimizálva van" esetekre (a korai return-ök ELŐTT nem történik írás, tehát biztonságosan nem commitolnak semmit).

- [ ] **Step 1: `accounts-store.ts` megírása**

```ts
import { randomUUID } from 'node:crypto'
import { prisma } from '@plantbase/db'
import { hashPassword } from '../lib/password.js'

export interface AccountSummary {
  id: number
  fullName: string
  email: string
  role: string
  anonymizedAt: string | null
}

export type AnonymizeResult = 'ok' | 'not_found' | 'already_anonymized'

export async function anonymizeAccount(
  accountId: number,
): Promise<AnonymizeResult> {
  return prisma.$transaction(async (tx) => {
    const existing = await tx.account.findUnique({
      where: { id: accountId },
    })
    if (!existing) return 'not_found'
    if (existing.anonymizedAt) return 'already_anonymized'

    // A hash bemenete egy véletlen, sosem tárolt érték -- a jelszó ezután
    // kriptográfiailag visszafejthetetlen, nem csak egy flag-gel letiltott.
    const passwordHash = await hashPassword(randomUUID())

    await tx.account.update({
      where: { id: accountId },
      data: {
        fullName: `Törölt felhasználó #${accountId}`,
        salutation: 'Ügyfél',
        email: `anonim-${accountId}@plantbase.hu`,
        passwordHash,
        anonymizedAt: new Date(),
      },
    })
    await tx.session.deleteMany({ where: { accountId } })

    return 'ok'
  })
}

export async function listAccountsForStaff(): Promise<AccountSummary[]> {
  const accounts = await prisma.account.findMany({
    orderBy: { id: 'asc' },
    select: {
      id: true,
      fullName: true,
      email: true,
      role: true,
      anonymizedAt: true,
    },
  })
  return accounts.map((a) => ({
    id: a.id,
    fullName: a.fullName,
    email: a.email,
    role: a.role,
    anonymizedAt: a.anonymizedAt ? a.anonymizedAt.toISOString() : null,
  }))
}
```

- [ ] **Step 2: teszt írása**

Az `orders-store.spec.ts` Prisma-mock mintáját követve (`vi.mock('@plantbase/db', ...)`), mockold a `../lib/password.js`-t is (`hashPassword: vi.fn()`):

```ts
vi.mock('../lib/password.js', () => ({ hashPassword: vi.fn() }))
```

Tesztek:
- `anonymizeAccount(id)` sikeres esetben: meghívja `prisma.account.update`-et a helyes placeholder-mezőkkel (`fullName`, `salutation`, `email` az `id`-ből számolva, `passwordHash` a mockolt `hashPassword` visszatérési értéke, `anonymizedAt` egy `Date`), meghívja `prisma.session.deleteMany`-t `{where:{accountId: id}}`-vel, és `'ok'`-ot ad vissza.
- `anonymizeAccount(id)` nemlétező fiókra: `prisma.account.findUnique` `null`-t ad vissza → `'not_found'`, `update`/`deleteMany` NINCS meghívva.
- `anonymizeAccount(id)` már anonimizált fiókra: `findUnique` egy `anonymizedAt: new Date(...)`-tel rendelkező fiókot ad vissza → `'already_anonymized'`, `update`/`deleteMany` NINCS meghívva.
- `listAccountsForStaff()` — meghívja `prisma.account.findMany`-t a helyes `select`/`orderBy` opciókkal, és a visszaadott sorokat helyesen alakítja `AccountSummary[]`-vé (`anonymizedAt: null` marad `null`, egy `Date` ISO-stringgé alakul).

Nézd meg a valódi `orders-store.spec.ts`-t/`escalations-store.spec.ts`-t a pontos mock-adatszerkezet mintájáért.

- [ ] **Step 3: tesztek + build futtatása**

Run: `pnpm exec nx run-many -t lint,test,build -p server`
Expected: minden zöld.

- [ ] **Step 4: Commit**

```bash
git add apps/server/src/lib/accounts-store.ts apps/server/src/lib/accounts-store.spec.ts
git commit -m "feat: add account anonymization store"
```

---

## Task 3: `apps/server` — staff-accounts végpontok + `app.ts` bekötés

**Files:**
- Create: `apps/server/src/routes/staff-accounts.ts`
- Test: `apps/server/src/routes/staff-accounts.spec.ts`
- Modify: `apps/server/src/app.ts`

**Interfaces:**
- Consumes: `anonymizeAccount`, `listAccountsForStaff` (`../lib/accounts-store.js`, Task 2); `requireAccount`, `requireRole` (meglévő middleware-ek).

- [ ] **Step 1: `staff-accounts.ts` route megírása**

```ts
import { Router } from 'express'
import { requireAccount } from '../middleware/session.js'
import { requireRole } from '../middleware/role.js'
import { anonymizeAccount, listAccountsForStaff } from '../lib/accounts-store.js'

function parseAccountId(raw: unknown): number | null {
  if (typeof raw !== 'string') return null
  const accountId = Number(raw)
  return Number.isInteger(accountId) ? accountId : null
}

export const staffAccountsRouter: Router = Router()

staffAccountsRouter.get(
  '/api/staff/accounts',
  requireAccount,
  requireRole('staff', 'admin'),
  async (_req, res) => {
    const accounts = await listAccountsForStaff()
    res.status(200).json({ accounts })
  },
)

staffAccountsRouter.post(
  '/api/staff/accounts/:id/anonymize',
  requireAccount,
  requireRole('staff', 'admin'),
  async (req, res) => {
    const accountId = parseAccountId(req.params.id)
    if (accountId === null) {
      res.status(400).json({ error: 'Érvénytelen fiók-azonosító.' })
      return
    }
    const result = await anonymizeAccount(accountId)
    if (result === 'not_found') {
      res.status(404).json({ error: 'Nem található ilyen fiók.' })
      return
    }
    if (result === 'already_anonymized') {
      res.status(409).json({ error: 'Ez a fiók már anonimizálva van.' })
      return
    }
    res.status(200).json({ ok: true })
  },
)
```

- [ ] **Step 2: teszt írása**

A `staff-escalations.spec.ts` supertest-mintáját követve, ellenőrizd mindkét végpontra:
- 401, ha nincs bejelentkezve.
- 403, ha `customer` szerepkörű a bejelentkezett fiók.
- `GET /api/staff/accounts`: 200 + a helyes JSON-alak, ha `staff`/`admin`.
- `POST /api/staff/accounts/:id/anonymize`: 400 érvénytelen `:id`-re; 404, ha a store `'not_found'`-ot ad; 409, ha `'already_anonymized'`-ot; 200 `{ok:true}`-val sikeres esetben.

Mockold a `../lib/accounts-store.js`-t (`anonymizeAccount: vi.fn()`, `listAccountsForStaff: vi.fn()`), a session/auth-mockolást a `staff-escalations.spec.ts`-ből vedd át pontosan.

- [ ] **Step 3: `app.ts` bővítése**

A jelenlegi router-regisztráció kapjon egy új sort a `staffMetricsRouter` UTÁN, az `errorHandler` ELŐTT:
```ts
import { staffAccountsRouter } from './routes/staff-accounts.js'
```
és az `app.use(staffMetricsRouter)` sor UTÁN:
```ts
app.use(staffAccountsRouter)
```
Olvasd be a jelenlegi valódi `app.ts`-et a módosítás előtt — ha eltér a fentitől, a valódi fájl a mérvadó.

- [ ] **Step 4: tesztek + build futtatása**

Run: `pnpm exec nx run-many -t lint,test,build -p server`
Expected: minden zöld.

- [ ] **Step 5: Commit**

```bash
git add apps/server/src/routes/staff-accounts.ts apps/server/src/routes/staff-accounts.spec.ts apps/server/src/app.ts
git commit -m "feat: add staff accounts list and anonymize endpoints"
```

---

## Task 4: `apps/server` — bejelentkezés-védelem anonimizált fiókra

**Files:**
- Modify: `apps/server/src/routes/auth.ts`
- Modify: `apps/server/src/routes/auth.spec.ts`

**Interfaces:** nincs — meglévő route-logika bővítése.

- [ ] **Step 1: `auth.ts` login handler bővítése**

A jelenlegi login-ellenőrzés:
```ts
  const account = await prisma.account.findUnique({ where: { email } })
  if (!account || !(await verifyPassword(password, account.passwordHash))) {
    res.status(401).json({ error: 'Hibás e-mail vagy jelszó.' })
    return
  }
```
Módosítsd, hogy az `anonymizedAt`-ot is ellenőrizze, UGYANAZZAL az általános üzenettel (ne áruljon el külön állapotot):
```ts
  const account = await prisma.account.findUnique({ where: { email } })
  if (
    !account ||
    account.anonymizedAt ||
    !(await verifyPassword(password, account.passwordHash))
  ) {
    res.status(401).json({ error: 'Hibás e-mail vagy jelszó.' })
    return
  }
```
Olvasd be a jelenlegi valódi `auth.ts`-et a módosítás előtt — ha eltér a fentitől, a valódi fájl a mérvadó, ehhez igazítsd a diffet.

- [ ] **Step 2: `auth.spec.ts` bővítése**

Adj hozzá egy új tesztet a meglévő login-tesztek mellé: egy `anonymizedAt`-tal rendelkező (mockolt) fiók helyes jelszóval való bejelentkezési kísérlete 401-et ad, `{error: 'Hibás e-mail vagy jelszó.'}`-val (UGYANAZZAL az üzenettel, mint a rossz-jelszó teszt-eset — ha van ilyen meglévő teszt, hasonlítsd össze a két hibaüzenetet explicit, hogy tényleg azonosak).

- [ ] **Step 3: tesztek + build futtatása**

Run: `pnpm exec nx run-many -t lint,test,build -p server`
Expected: minden zöld.

- [ ] **Step 4: Commit**

```bash
git add apps/server/src/routes/auth.ts apps/server/src/routes/auth.spec.ts
git commit -m "fix: refuse login for anonymized accounts"
```

---

## Task 5: `packages/db` — e-mail-fájlok megőrzési script

**Files:**
- Create: `packages/db/prisma/cleanup-old-emails.ts`
- Test: `packages/db/prisma/cleanup-old-emails.spec.ts`

**Interfaces:**
- Produces: `cleanupOldEmails(emailsDir?: string, retentionDays?: number): Promise<string[]>` (a törölt fájlnevek listájával tér vissza) — exportálva teszteléshez.

**FONTOS — útvonal-mélység, ELLENŐRIZENDŐ, nem feltételezendő:** ez a fájl `packages/db/prisma/`-ban van, NEM `packages/core/src/lib/email/`-ben (ahol a `send-simulated-email.ts` van, 5 szinttel a repo gyökér felett) — a mélység itt MÁS. `packages/db/prisma/cleanup-old-emails.ts` saját könyvtára (`packages/db/prisma`) a repo gyökerétől 3 szinttel van lejjebb (`prisma` → `db` → `packages` → gyökér), tehát a helyes relatív útvonal `'../../../emails'` (3 `..` szegmens), NEM 5. Ezt a Step 1 kódja már helyesen tartalmazza, de a Step 3 kézi ellenőrzése kötelező — ne csak a mockolt tesztre hagyatkozz.

- [ ] **Step 1: `cleanup-old-emails.ts` megírása**

```ts
import { readdir, stat, unlink } from 'node:fs/promises'
import { dirname, join } from 'node:path'
import { fileURLToPath } from 'node:url'

// packages/db/prisma/ -> 3 szint fel a repo gyökeréig: prisma -> db -> packages -> repo-gyökér.
const DEFAULT_EMAILS_DIR = join(
  dirname(fileURLToPath(import.meta.url)),
  '../../../emails',
)
const DEFAULT_RETENTION_DAYS = 30

export async function cleanupOldEmails(
  emailsDir: string = DEFAULT_EMAILS_DIR,
  retentionDays: number = DEFAULT_RETENTION_DAYS,
): Promise<string[]> {
  const cutoff = Date.now() - retentionDays * 24 * 60 * 60 * 1000
  let entries: string[]
  try {
    entries = await readdir(emailsDir)
  } catch (err) {
    if ((err as NodeJS.ErrnoException).code === 'ENOENT') return []
    throw err
  }

  const deleted: string[] = []
  for (const entry of entries) {
    if (!entry.endsWith('.md')) continue
    const filePath = join(emailsDir, entry)
    const stats = await stat(filePath)
    if (stats.mtimeMs < cutoff) {
      await unlink(filePath)
      deleted.push(entry)
    }
  }
  return deleted
}

async function main(): Promise<void> {
  const deleted = await cleanupOldEmails()
  if (deleted.length === 0) {
    console.log('Nincs törlendő e-mail-fájl.')
  } else {
    console.log(`${deleted.length} e-mail-fájl törölve:`)
    for (const name of deleted) console.log(`  - ${name}`)
  }
}

// Csak közvetlen futtatáskor (tsx-szel) fusson le automatikusan -- NE akkor,
// ha egy teszt importálja a cleanupOldEmails függvényt. Ez eltér a
// seed-staff.ts mintájától (ott nincs guard), mert ide egy önálló,
// a valódi /emails könyvtárat NEM érintő tesztnek importálhatónak kell
// lennie. Windows-on is ellenőrizendő (ismert kockázat: az
// import.meta.url/process.argv[1] összehasonlítás elvi platformfüggő
// eltérése) -- lásd Step 3.
if (import.meta.url === `file://${process.argv[1]}`) {
  void main()
}
```

- [ ] **Step 2: teszt írása**

Valódi ideiglenes könyvtárban (nem mockolt `fs`), a `send-simulated-email.spec.ts` `mkdtemp`/`rm` mintáját követve:

```ts
import { mkdtemp, rm, writeFile, utimes } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { cleanupOldEmails } from './cleanup-old-emails.js'

describe('cleanupOldEmails', () => {
  let dir: string

  beforeEach(async () => {
    dir = await mkdtemp(join(tmpdir(), 'plantbase-cleanup-test-'))
  })

  afterEach(async () => {
    await rm(dir, { recursive: true, force: true })
  })

  it('deletes .md files older than the retention period, keeps recent ones', async () => {
    const oldFile = join(dir, 'email_old_20260101_1200.md')
    const recentFile = join(dir, 'email_recent_20260819_1200.md')
    await writeFile(oldFile, 'régi tartalom')
    await writeFile(recentFile, 'friss tartalom')

    const fortyDaysAgo = new Date(Date.now() - 40 * 24 * 60 * 60 * 1000)
    await utimes(oldFile, fortyDaysAgo, fortyDaysAgo)
    // recentFile mtime-ja a writeFile miatt "most" -- nincs mit módosítani rajta.

    const deleted = await cleanupOldEmails(dir, 30)

    expect(deleted).toEqual(['email_old_20260101_1200.md'])
    await expect(
      rm(oldFile, { force: false }).catch((e) => e.code),
    ).resolves.toBe('ENOENT')
    await expect(
      writeFile(recentFile, 'still there', { flag: 'r+' }),
    ).resolves.toBeUndefined()
  })

  it('ignores non-.md files even if old', async () => {
    const oldNonMd = join(dir, 'not-an-email.txt')
    await writeFile(oldNonMd, 'x')
    const longAgo = new Date(Date.now() - 100 * 24 * 60 * 60 * 1000)
    await utimes(oldNonMd, longAgo, longAgo)

    const deleted = await cleanupOldEmails(dir, 30)

    expect(deleted).toEqual([])
  })

  it('returns an empty array if the directory does not exist, without throwing', async () => {
    const missingDir = join(dir, 'nincs-ilyen-alkonyvtar')

    await expect(cleanupOldEmails(missingDir, 30)).resolves.toEqual([])
  })

  it('resolves the default emailsDir to the real repo-root emails/ directory', async () => {
    // Nincs emailsDir megadva -- ellenőrizd, hogy a DEFAULT_EMAILS_DIR
    // ténylegesen a repo gyökerén levő emails/ mappára mutat, NEM egy
    // packages/db-n belüli, rossz mélységű útvonalra. A brief FONTOS
    // szakaszában leírt 3-szintes számítást hasonlítsd össze egy a
    // teszt-fájl SAJÁT import.meta.url-jéből (ugyanabból a könyvtárból)
    // függetlenül kiszámolt várt úttal -- ez egy nem-tautologikus
    // ellenőrzés, mert a várt utat itt külön számoljuk, nem az
    // implementációból importáljuk. NE hozz létre/törölj semmit a valódi
    // emails/ mappában -- ez a teszt csak a DEFAULT_EMAILS_DIR értékét
    // ellenőrzi közvetve (pl. egy re-export vagy egy kis refaktor révén,
    // ha szükséges ahhoz, hogy a konstans tesztelhető legyen anélkül,
    // hogy a valódi könyvtárat érintené).
  })
})
```

Az utolsó teszt csak vázlat — írd meg ténylegesen (töröld a placeholder-kommentet, mint a C al-projekt Task 1-jében), és győződj meg róla, hogy a `DEFAULT_EMAILS_DIR` tényleg a repo gyökerén lévő valódi `emails/` mappára mutat. Ha a jelenlegi kód nem exportálja közvetlenül a konstanst, exportáld (`export const DEFAULT_EMAILS_DIR = ...`), hogy a teszt közvetlenül összehasonlíthassa egy függetlenül számolt várt értékkel, a valódi fájlrendszer érintése nélkül.

- [ ] **Step 3: kézi ellenőrzés — útvonal-mélység és a guard**

Futtasd le a scriptet közvetlenül, és ellenőrizd, hogy a VALÓDI `emails/` könyvtárat éri el (nem egy rossz mélységű, nemlétező útvonalt):
```bash
pnpm --filter @plantbase/db exec tsx prisma/cleanup-old-emails.ts
```
Elvárt: vagy "Nincs törlendő e-mail-fájl." (ha a meglévő fájlok mind frissek), vagy egy törölt-fájlok-listája — NEM egy `ENOENT`-ből fakadó hibás/üres eredmény, ami rossz útvonalra utalna. Ha a valódi `emails/` könyvtárban vannak 30 napnál régebbi fájlok korábbi al-projektek E2E-futtatásaiból, ez a lépés TÉNYLEGESEN törölni fogja őket — ha ez nem kívánatos (pl. meg akarod őrizni őket demonstrációhoz), előbb másold ki őket egy ideiglenes helyre, vagy állítsd át ideiglenesen a retenciós napok számát futtatáskor.

Ezután ellenőrizd, hogy a teszt-futtatás (Step 2) NEM váltja ki a fenti automatikus futást (nincs konzol-kiírás, nincs valódi fájltörlés a `packages/db/prisma`-n kívüli, ideiglenes teszt-könyvtárakon túl).

- [ ] **Step 4: tesztek + build futtatása**

Run: `pnpm exec nx run-many -t lint,test,build -p db`
Expected: minden zöld.

- [ ] **Step 5: Commit**

```bash
git add packages/db/prisma/cleanup-old-emails.ts packages/db/prisma/cleanup-old-emails.spec.ts
git commit -m "feat: add simulated email retention cleanup script"
```

---

## Task 6: `docs/adatvedelem.md` — adatfolyam-dokumentáció

**Files:**
- Create: `docs/adatvedelem.md`

**Interfaces:** nincs — tiszta dokumentáció, más task-tal nem ütközhet.

- [ ] **Step 1: `docs/adatvedelem.md` megírása**

A tartalom a TÉNYLEGES kódbázisból ellenőrzött adatfolyamokat írja le (ne feltételezz, ellenőrizd a hivatkozott fájlokat, mielőtt leírod):

```markdown
# Plantbase — adatfolyam és adatvédelem

> Hol fut a modell, hova utazik az adat, mi nem hagyja el a saját környezetünket — a `docs/extension-for-customers.md` 87. sorának megfelelően.

## Külső szolgáltatások

| Szolgáltatás | Modell/API | Mit kap | Melyik kódútvonal |
| --- | --- | --- | --- |
| Anthropic API | `claude-sonnet-5` | A teljes chat-beszélgetés (ügyfél üzenetei + agent válaszai + tool-hívások eredményei) | `packages/core/src/lib/stream-agent.ts` (web-chat), `packages/core/src/lib/ask-agent.ts` (CLI) |
| Anthropic API | `claude-sonnet-5` | Az ügyfél neve/megszólítása + a rendelés adatai (a rendelés-visszaigazoló e-mail összeállításához) | `packages/core/src/lib/email/compose-order-email.ts` |
| OpenAI API | embedding modell | A növénygondozási kérdés szövege (beágyazáshoz) | `packages/core/src/lib/knowledge/embed-openai.ts`, `search-knowledge.ts` |
| OpenAI API | (HyDE lekérdezés-bővítéshez használt modell) | A növénygondozási kérdés szövege | `packages/core/src/lib/knowledge/hyde.ts` |

## Ami SOSEM hagyja el a saját környezetünket

- Az eszkalációs e-mail tartalma — SZÁNDÉKOSAN nincs hozzá LLM-hívás (`packages/core/src/lib/email/... ` nem tartalmazza; lásd `apps/server/src/lib/escalation-emails.ts` — determinisztikus sablon, csak DB-adatból), a D al-projekt biztonsági döntése alapján.
- A teljes `accounts` tábla (név, e-mail, jelszó-hash) — csak a helyi Postgres-ben.
- Az `orders`/`order_audit_log`/`escalations` táblák — csak a helyi Postgres-ben (az eszkaláció `summary` mezője az agent saját, már meglévő beszélgetés-kontextusból származó összefoglalója, nem egy külön kimenő hívás eredménye).
- A `products`/`knowledge_chunks` katalógus-adat — csak a helyi Postgres-ben (a `knowledge_chunks.embedding` mező MÁR a beágyazott, OpenAI-nál előállított vektor, de az eredeti forrás-szöveg a helyi adatbázisban van).
- A szimulált e-mail-fájlok (`/emails`) — csak a helyi fájlrendszeren.

## Anonimizálás / adat-eltűnés

Lásd `docs/superpowers/specs/2026-08-20-privacy-design.md` — staff-indított, irreverzibilis fiók-anonimizálás (`POST /api/staff/accounts/:id/anonymize`) + az `/emails` könyvtár időkorlátos automatikus törlése (`packages/db/prisma/cleanup-old-emails.ts`, alapértelmezetten 30 nap).
```

Ellenőrizd a táblázat minden sorát a hivatkozott fájl tényleges tartalma alapján, mielőtt commitolod — különösen azt, hogy a HyDE-hívás valóban egy másik/ugyanaz OpenAI-modellt hív-e, és hogy nincs-e egyéb, a fentiekben fel nem sorolt külső hívás valahol a kódbázisban (gyors `grep`-pel `anthropic(`/`openai(`/`createOpenAI`/`@ai-sdk` mintákra a teljes `packages/core/src`-ben, hogy semmi ne maradjon ki).

- [ ] **Step 2: Commit**

```bash
git add docs/adatvedelem.md
git commit -m "docs: document data flow to external LLM providers"
```

---

## Task 7: `apps/web` — staff fiók-lista + anonimizálás gomb

**Files:**
- Create: `apps/web/src/lib/accounts-api-client.ts`
- Create: `apps/web/src/pages/staff-accounts-page.tsx`

**Interfaces:**
- Consumes: `GET /api/staff/accounts`, `POST /api/staff/accounts/:id/anonymize` (Task 3).
- Produces: `StaffAccountsPage` React komponens, amit Task 8 (`app.tsx`) fog importálni.

- [ ] **Step 1: `accounts-api-client.ts` megírása**

```ts
export interface StaffAccount {
  id: number
  fullName: string
  email: string
  role: string
  anonymizedAt: string | null
}

async function parseJsonOrThrow(response: Response): Promise<unknown> {
  const body = await response.json().catch(() => ({}))
  if (!response.ok) {
    const message =
      (body as { error?: string }).error ?? 'Ismeretlen hiba történt.'
    throw new Error(message)
  }
  return body
}

export async function listStaffAccounts(): Promise<StaffAccount[]> {
  const response = await fetch('/api/staff/accounts', {
    credentials: 'include',
  })
  const body = (await parseJsonOrThrow(response)) as {
    accounts: StaffAccount[]
  }
  return body.accounts
}

export async function anonymizeStaffAccount(accountId: number): Promise<void> {
  const response = await fetch(`/api/staff/accounts/${accountId}/anonymize`, {
    method: 'POST',
    credentials: 'include',
  })
  await parseJsonOrThrow(response)
}
```

- [ ] **Step 2: `staff-accounts-page.tsx` megírása**

A `staff-escalations-page.tsx` szerkezetét/stílusát követve, egy "Művelet" oszloppal, ami vagy egy "Anonimizálás" gombot mutat (megerősítő `window.confirm()`-mel, mivel irreverzibilis), vagy egy státusz-szöveget, ha a fiók már anonimizálva van:

```tsx
import { useEffect, useState } from 'react'
import {
  listStaffAccounts,
  anonymizeStaffAccount,
  type StaffAccount,
} from '../lib/accounts-api-client.js'
import { logout } from '../lib/api-client.js'
import { useAuth } from '../lib/auth-context.js'
import { Button } from '../components/ui/button.js'
import { Card, CardContent, CardHeader } from '../components/ui/card.js'

export function StaffAccountsPage() {
  const { account, refresh } = useAuth()
  const [accounts, setAccounts] = useState<StaffAccount[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)

  async function load() {
    setLoading(true)
    setError(null)
    try {
      setAccounts(await listStaffAccounts())
    } catch (err) {
      setError(
        err instanceof Error ? err.message : 'Ismeretlen hiba történt.',
      )
    } finally {
      setLoading(false)
    }
  }

  useEffect(() => {
    void load()
  }, [])

  async function handleAnonymize(accountId: number) {
    if (
      !window.confirm(
        'Biztosan anonimizálod ezt a fiókot? Ez a művelet nem vonható vissza.',
      )
    ) {
      return
    }
    try {
      await anonymizeStaffAccount(accountId)
      await load()
    } catch (err) {
      setError(
        err instanceof Error ? err.message : 'Ismeretlen hiba történt.',
      )
    }
  }

  async function handleLogout() {
    await logout()
    await refresh()
  }

  return (
    <div className="mx-auto max-w-4xl p-4">
      <div className="mb-4 flex items-center justify-between">
        <h1 className="text-lg font-semibold">
          Fiókok ({account?.salutation})
        </h1>
        <Button variant="outline" size="sm" onClick={handleLogout}>
          Kilépés
        </Button>
      </div>
      {error && <p className="text-sm text-red-600">{error}</p>}
      {loading ? (
        <p>Betöltés...</p>
      ) : (
        <Card>
          <CardHeader>
            <span className="text-sm text-gray-500">
              {accounts.length} fiók
            </span>
          </CardHeader>
          <CardContent>
            <table className="w-full text-sm">
              <thead>
                <tr className="text-left text-gray-500">
                  <th className="pb-2">#</th>
                  <th className="pb-2">Név</th>
                  <th className="pb-2">E-mail</th>
                  <th className="pb-2">Szerepkör</th>
                  <th className="pb-2">Művelet</th>
                </tr>
              </thead>
              <tbody>
                {accounts.map((acc) => (
                  <tr key={acc.id} className="border-t border-gray-100">
                    <td className="py-2">{acc.id}</td>
                    <td className="py-2">{acc.fullName}</td>
                    <td className="py-2">{acc.email}</td>
                    <td className="py-2">{acc.role}</td>
                    <td className="py-2">
                      {acc.anonymizedAt ? (
                        <span className="text-gray-400">Anonimizálva</span>
                      ) : (
                        <Button
                          variant="outline"
                          size="sm"
                          onClick={() => handleAnonymize(acc.id)}
                        >
                          Anonimizálás
                        </Button>
                      )}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </CardContent>
        </Card>
      )}
    </div>
  )
}
```

- [ ] **Step 3: lint + build futtatása**

Run: `pnpm exec nx run-many -t lint,build -p web`
Expected: hiba nélkül. (Nincs automatizált teszt ehhez a fájlpárhoz — a repo precedense szerint, a staff-webfelület manuális ellenőrzéssel záródik, az utolsó tasknál.)

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/lib/accounts-api-client.ts apps/web/src/pages/staff-accounts-page.tsx
git commit -m "feat: add staff accounts list page with anonymize action"
```

---

## Task 8: `apps/web` — navigáció bővítése negyedik füllel

**Files:**
- Modify: `apps/web/src/app.tsx`

**Interfaces:**
- Consumes: `StaffOrdersPage`/`StaffEscalationsPage`/`StaffMetricsPage` (meglévők), `StaffAccountsPage` (Task 7).

**FONTOS:** ez az egyetlen task, ami `app.tsx`-et módosítja ebben a tervben.

- [ ] **Step 1: `app.tsx` bővítése**

A jelenlegi `StaffArea` komponens négy tab helyett hármat kezel (`'orders' | 'escalations' | 'metrics'`). Olvasd be a jelenlegi valódi fájlt, majd bővítsd `'accounts'`-szal:
```tsx
function StaffArea() {
  const [tab, setTab] = useState<
    'orders' | 'escalations' | 'metrics' | 'accounts'
  >('orders')

  return (
    <div>
      <div className="mx-auto flex max-w-4xl gap-2 px-4 pt-4">
        <Button
          variant={tab === 'orders' ? 'default' : 'outline'}
          size="sm"
          onClick={() => setTab('orders')}
        >
          Rendelések
        </Button>
        <Button
          variant={tab === 'escalations' ? 'default' : 'outline'}
          size="sm"
          onClick={() => setTab('escalations')}
        >
          Eszkalációk
        </Button>
        <Button
          variant={tab === 'metrics' ? 'default' : 'outline'}
          size="sm"
          onClick={() => setTab('metrics')}
        >
          Metrikák
        </Button>
        <Button
          variant={tab === 'accounts' ? 'default' : 'outline'}
          size="sm"
          onClick={() => setTab('accounts')}
        >
          Fiókok
        </Button>
      </div>
      {tab === 'orders' && <StaffOrdersPage />}
      {tab === 'escalations' && <StaffEscalationsPage />}
      {tab === 'metrics' && <StaffMetricsPage />}
      {tab === 'accounts' && <StaffAccountsPage />}
    </div>
  )
}
```
Add hozzá az importot a fájl tetején, a többi staff-oldal import mellé:
```ts
import { StaffAccountsPage } from './pages/staff-accounts-page.js'
```

- [ ] **Step 2: lint + build futtatása**

Run: `pnpm exec nx run-many -t lint,build -p web`
Expected: hiba nélkül.

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/app.tsx
git commit -m "feat: add accounts tab to staff navigation"
```

---

## Task 9: teljes workspace ellenőrzés + manuális E2E

**Files:** nincs kódváltozás — csak ellenőrzés (B/C/D/E záró task-jainak mintájára).

- [ ] **Step 1: Teljes workspace build/lint/teszt**

```bash
pnpm exec nx run-many -t lint,test,build -p core,db,server,web,cli
```
Expected: minden zöld, KIVÉVE a már ismert, korábbi al-projektek előtt is meglévő hibák (a `@plantbase/db:lint` 3 dokumentált hibája) — ezek nem ehhez a munkához tartoznak, ne javítsd őket.

- [ ] **Step 2: Manuális E2E — e-mail-takarítás**

Ha a Task 5 Step 3 még nem futott le a valódi `/emails` könyvtáron (vagy ha újra ellenőrizni akarod), futtasd le újra:
```bash
pnpm --filter @plantbase/db exec tsx prisma/cleanup-old-emails.ts
```
Ellenőrizd az eredményt (törölt fájlok listája vagy "Nincs törlendő e-mail-fájl.").

- [ ] **Step 3: Manuális E2E — fiók-anonimizálás**

```bash
pnpm exec nx run server:serve
pnpm exec nx run web:serve
```
Regisztrálj egy ÚJ, kifejezetten teszt-célú `customer` fiókot (hogy ne érints egy korábbi al-projekt E2E-adatait). Jelentkezz be `staff@plantbase.hu` / `Staff1234`-gyel, nyisd meg a "Fiókok" fület, ellenőrizd, hogy a listában szerepel az új teszt-fiók. Kattints az "Anonimizálás" gombra, erősítsd meg. Ellenőrizd:
- A lista frissül, a fiók neve/e-mail-je placeholder-re változik, "Anonimizálva" státuszt mutat gomb helyett.
- Adatbázisban közvetlenül is ellenőrizhető (`pnpm --filter @plantbase/db exec tsx -e "..."`, `prisma` a `./src/lib/client.js`-ből): `fullName`/`salutation`/`email` a várt placeholder-mintázatot mutatja, `anonymizedAt` nem `null`.
- Az anonimizált fiók e-mail-jével/eredeti jelszavával bejelentkezési kísérlet 401-et ad (`Hibás e-mail vagy jelszó.`).
- Ha a teszt-fióknak volt korábban (az anonimizálás előtt) rendelése/eszkalációja, azok a staff Rendelések/Eszkalációk fülön TOVÁBBRA IS láthatók, de az `accountId`-hoz tartozó fiók már a scrub-olt adatokat mutatja (a rendelés/eszkaláció saját mezői — pl. `orderDesc` — változatlanok maradnak, csak a hozzá tartozó FIÓK anonim).

- [ ] **Step 4: Manuális E2E — idempotencia + jogosultság**

Próbáld meg ÚJRA anonimizálni ugyanazt a fiókot (pl. közvetlen API-hívással, mivel a UI már elrejti a gombot) → 409, `{error: 'Ez a fiók már anonimizálva van.'}`. `customer` fiókkal próbáld elérni közvetlenül a `GET /api/staff/accounts` vagy `POST /api/staff/accounts/:id/anonymize` végpontokat → 403 mindkettőre.

- [ ] **Step 5: Eredmény rögzítése**

Ha bármelyik lépés hibát mutat, a hibát az érintett Task fájljaiban oldd meg, majd ismételd meg ezt a Task 9-et.

Ha minden lépés sikeres, ez a Task 9 zárja az F al-projektet — és ezzel a `docs/extension-for-customers.md`-ben leírt teljes A-F felbontást. Nincs commit (nincs kódváltozás), de jelezd a felhasználónak, hogy F kész, és hogy ez volt az utolsó al-projekt a felbontásban.
